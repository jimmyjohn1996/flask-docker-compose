services:

  web:
    build: .
    container_name: flask_app
    ports:
      - "5001:5000"
    environment:
      DB_HOST: restaurant-db.c0146eoywp46.us-east-1.rds.amazonaws.com
      DB_PORT: 5432
      DB_NAME: restaurant
      DB_USER: postgres
      DB_PASS: "password"
    restart: always
